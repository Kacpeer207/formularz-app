import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // lub usuń, jeśli nie używasz
import App from './App'; // Upewnij się, że ścieżka jest poprawna

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
